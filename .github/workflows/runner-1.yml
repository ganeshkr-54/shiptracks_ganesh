name: Show Runner IP

on:
  push:
    branches: [main, master]

jobs:
  show-ip:
    runs-on: [self-hosted, linux, x64]
    steps:
      - name: Show public IP
        run: |
          whoami
          echo "Public IP:"
          curl -s https://api.ipify.org
          echo
