name: Show Runner IP

on:
  push:
    branches: [main, master]

jobs:
  show-ip:
    runs-on: [self-hosted, linux, x64]
    steps:
      - name: Verify state dir
        run: |
          whoami
          id
          ls /
